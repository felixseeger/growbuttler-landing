# Memory: 2026-02-27 - GrowButtler Core Implementation

## Accomplishments
- **New Design Implementation:** Built and deployed three complex UI pages from Figma designs:
    - `Journal New Entry`: `/journal/new-entry` (Photo upload, vitals sliders, nutrient logs).
    - `Consultation Success`: `/consultation/success` (Success confirmation with expert summary).
    - `Expert Profile`: `/expert/[id]` (Dr. Aris Thorne sample, stats grid, biography, services list).
- **Authentication & User Creation:**
    - Developed `Signup` and `Login` pages.
    - Built a Next.js API route (`/api/signup`) that bridges to WordPress REST API for secure user creation (roles: `subscriber` by default).
    - Fixed Application Password handling (cleaning whitespace).
- **Expert Vetting Flow:**
    - Implemented a multi-step Expert Application flow at `/experts/apply` (Identity -> Experience -> Portfolio -> Interview Scheduling).
    - Created API route (`/api/experts/apply`) to handle submissions, store expert metadata in WordPress, and assign `expert_applicant` role.
- **Email Infrastructure:**
    - Integrated **Resend** for automated transactional emails.
    - Configured templates for: Welcome Email, Expert Application Received (Applicant), and New Application Alert (Admin).
- **WordPress Data Fetching Fixes:**
    - Corrected `nav.links` handling to support both array and single link objects from ACF.
    - Fixed `Features` section title/subtitle merging logic to prefer ACF values.
- **Deployment:** Successfully deployed all 16 routes to Vercel production: `https://growbuttler.felixseeger.de`.

## Decisions & Logic
- **Expert Status:** Experts are registered with the `expert_applicant` role. The directory is configured to filter for `application_status == 'approved'` (to be fully integrated into the query).
- **Username Strategy:** Using `email` as the WordPress `username` during signup for simplicity.

## Next Steps
- Implement JWT/Session logic for the Login page.
- Complete the Dashboard (Plant Management Hub) UI.
- Link "Book Now" buttons to a booking/payment flow.
- Ensure `RESEND_API_KEY` is added to Vercel environment variables for live emails.
